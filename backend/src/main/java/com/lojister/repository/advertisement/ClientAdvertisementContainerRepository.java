package com.lojister.repository.advertisement;

import com.lojister.model.entity.client.ClientAdvertisementContainer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClientAdvertisementContainerRepository extends JpaRepository<ClientAdvertisementContainer,Long> {
}
